# Models Directory

This directory contains shared domain models used throughout the app.

## Contents
- `game_settings.dart`: Represents player preferences (music volume, sfx volume, screen controls).
- `game_progress.dart`: Represents player save slot data (score, coins, current level, inventory, completed achievements).
- `save_slot.dart`: Encapsulates a save file (metadata, date, snapshot details).
