# UI Directory

This directory contains application UI widgets, design configurations, routes, and layout helpers.

## Contents
- `router.dart`: GoRouter setup defining transitions and pages.
- `theme.dart`: Color palettes, typography styles, and buttons.
- `widgets/`: Reusable widgets like volume sliders, overlay container cards, retro borders.
